"use strict";

// Časté dotazy (юнит U11) — своей логики открытия/закрытия не нужно: аккордеон полностью
// покрывает библиотека bayan (`components.b/groupers/_bayan.js`, уже импортирована в
// `index.js`). Первый вопрос раскрыт по умолчанию классом `bayan--opened` прямо в разметке
// (`src/tpls/sections/faq.html`), клик/тоггл — на самом bayan. Файл оставлен пустым
// намеренно (см. конвенцию файлов секции в `pages/page-homepage.md`), а не удалён.
